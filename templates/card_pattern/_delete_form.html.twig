<form method="post" action="{{ path('card_pattern_delete', {'id': card_pattern.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ card_pattern.id) }}">
    <button class="btn">Delete</button>
</form>
