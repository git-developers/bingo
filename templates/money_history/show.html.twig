{% extends 'base.html.twig' %}

{% block title %}MoneyHistory{% endblock %}

{% block body %}
    <h1>MoneyHistory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ money_history.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ money_history.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ money_history.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('money_history_index') }}">back to list</a>

    <a href="{{ path('money_history_edit', {'id': money_history.id}) }}">edit</a>

    {{ include('money_history/_delete_form.html.twig') }}
{% endblock %}
